<h1 class="text-center">Photo Gallery</h1>
<% @albums.each do |alb| %>
	<div class="border border-3 text-center">
		<h2><%=alb.title %></h2>
		<p><%= link_to site_photo_path(id: alb.id) %></p>
		<p><%= raw alb.description %></p>
	</div>
<% end %>