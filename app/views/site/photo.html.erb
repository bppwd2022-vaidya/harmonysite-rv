<h1 class="text-center">Photos</h1>
<h2 class="text-center py-5"><%= @album.title %></h2>
	<% @album.photos.each do |photo| %>
		<div class="border border-3 p-5">
				<h4 class="text-center pt-3"><%= photo.title %></h4>
				<p><%= photo.description %></p>
		</div>
	<% end %>